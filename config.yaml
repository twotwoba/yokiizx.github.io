baseurl: 'https://yokiizx.site'
languageCode: 'en-us'
title: 'Yokiizx'
theme: 'hugo-tania'
paginate: 6
copyright: 'CopyRight © 2022-{year} yokiizx. All rights reserved.'

disqusShortname: https-hugo-tania-netlify-app

params:
  # Emoji will show before the blog title on site navbar.
  # titleEmoji: "🍭"

  # Logo will show before the blog title on site navbar.
  titleLogo: favicon.ico

  # Enable float footnotes.
  # Default to true
  enableFootnotes: true

  siteName: 'yokiizx的小破站'
  siteDesc: '前端 react vue typescript JavaScript'
  author: 'yokiizx'

  busuanzi:
    enable: false

  colorScheme:
    # Enable toggle colorScheme
    # Default to true
    toggle: true
    # Default colorScheme
    # Default to auto
    default: auto

  # Limit how many categories filter show above search input.
  # Default to 5
  maxCategoryToShow: 5

  # Show your socail information with icon on index bio with `_index.md` content.
  socialOptions:
    dev-to:
    github: https://github.com/yokiizx
    email: yokiizx@163.com
    facebook:
    instagram:
    linkedin:
    medium:
    stack-overflow:
    steam:
    telegram:
    twitter:
    twitch:
    whatsapp:

  # Comments settings
  comments:
    enabled: true
    provider: giscus

    giscus:
      repo: yokiizx/blog-comment
      id: R_kgDOIAftXg
      category:
        name: Announcements
        id: DIC_kwDOIAftXs4CRdqE

menu:
  header:
    - name: Articles
      url: '/articles/'
      weight: 1
    - name: Tags
      url: '/tags/'
      weight: 2
    - name: Notes
      url: 'notes'
      weight: 3
  # footer:
  #   - name: RSS
  #     url: "/index.xml"

markup:
  highlight:
    noClasses: false
    lineNos: true
  goldmark:
    renderer:
      unsafe: true

outputs:
  home: ['HTML', 'JSON']
  # home: ["HTML", "RSS", "JSON"]
